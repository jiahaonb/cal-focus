<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="AppBrandColor">#0D5D56</Color>
    <Color x:Key="AppBackgroundColor">#EEF3F1</Color>
    <Color x:Key="AppSurfaceColor">#F6FBF9</Color>
    <Color x:Key="AppCardColor">#FAFFFFFF</Color>
    <Color x:Key="AppCardHoverColor">#FFFFFFFF</Color>
    <Color x:Key="AppTextColor">#1B2A27</Color>
    <Color x:Key="AppMutedColor">#6F8580</Color>
    <Color x:Key="AppBorderColor">#44FFFFFF</Color>
    <Color x:Key="AppTodayHighlightColor">#263AC6B8</Color>

    <SolidColorBrush x:Key="AppBrandBrush" Color="{StaticResource AppBrandColor}" />
    <SolidColorBrush x:Key="AppBackgroundBrush" Color="{StaticResource AppBackgroundColor}" />
    <SolidColorBrush x:Key="AppSurfaceBrush" Color="{StaticResource AppSurfaceColor}" />
    <SolidColorBrush x:Key="AppCardBrush" Color="{StaticResource AppCardColor}" />
    <SolidColorBrush x:Key="AppCardHoverBrush" Color="{StaticResource AppCardHoverColor}" />
    <SolidColorBrush x:Key="AppTextBrush" Color="{StaticResource AppTextColor}" />
    <SolidColorBrush x:Key="AppTitleBrush" Color="{StaticResource AppTextColor}" />
    <SolidColorBrush x:Key="AppMutedBrush" Color="{StaticResource AppMutedColor}" />
    <SolidColorBrush x:Key="AppBorderBrush" Color="{StaticResource AppBorderColor}" />
    <SolidColorBrush x:Key="AppTodayHighlightBrush" Color="{StaticResource AppTodayHighlightColor}" />

    <SolidColorBrush x:Key="AppGlassBrush" Color="#E8FFFFFF" />

    <Style x:Key="PageRootGridStyle" TargetType="Grid">
        <Setter Property="Background" Value="{ThemeResource AppBackgroundBrush}" />
    </Style>

    <Style x:Key="GlassCardStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="18" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{ThemeResource AppBorderBrush}" />
        <Setter Property="Background" Value="{ThemeResource AppGlassBrush}" />
    </Style>

    <Style x:Key="SurfaceCardStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="18" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{ThemeResource AppBorderBrush}" />
        <Setter Property="Background" Value="{ThemeResource AppCardBrush}" />
    </Style>

    <Style x:Key="SectionTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="22" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{ThemeResource AppBrandBrush}" />
    </Style>

    <Style x:Key="SectionHintTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{ThemeResource AppMutedBrush}" />
    </Style>

    <Style x:Key="PrimaryActionButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Background" Value="{ThemeResource AppBrandBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="18,10" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="SecondaryActionButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{ThemeResource AppTextBrush}" />
        <Setter Property="Background" Value="{ThemeResource AppCardBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource AppBrandBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="SoftTagBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource AppTodayHighlightBrush}" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="8,3" />
    </Style>

    <Style x:Key="CardListItemContainerStyle" TargetType="ListViewItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0,0,0,10" />
    </Style>
</ResourceDictionary>
