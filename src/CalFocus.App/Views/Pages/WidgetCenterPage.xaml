<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="CalFocus.App.Views.Pages.WidgetCenterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">
    <Grid Style="{StaticResource PageRootGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Padding="24,24,24,12" Spacing="4">
            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="小组件中心" />
            <TextBlock Style="{StaticResource SectionHintTextStyle}" Text="选择一个静态预览卡片即可添加对应桌面组件。" />
            <TextBlock x:Name="WidgetCountText" FontSize="12" Foreground="{ThemeResource AppMutedBrush}" Text="当前数量：0" />
        </StackPanel>

        <ScrollViewer Grid.Row="1"
                      Padding="24,0,24,24"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Enabled"
                      HorizontalScrollBarVisibility="Disabled"
                      HorizontalScrollMode="Disabled"
                      ZoomMode="Disabled"
                      IsVerticalRailEnabled="True"
                      IsHorizontalRailEnabled="False">
            <GridView x:Name="WidgetPreviewGrid"
                      IsItemClickEnabled="False"
                      SelectionMode="None"
                      HorizontalAlignment="Stretch"
                      ScrollViewer.VerticalScrollMode="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Disabled"
                      ScrollViewer.HorizontalScrollMode="Disabled"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource GlassCardStyle}" Width="320" Margin="0,0,12,12">
                            <StackPanel Spacing="10">
                                <Border Height="120" Style="{StaticResource SurfaceCardStyle}" Padding="10">
                                    <StackPanel Spacing="4">
                                        <TextBlock FontSize="12" Foreground="{ThemeResource AppMutedBrush}" Text="{Binding PreviewLabel}" />
                                        <TextBlock FontSize="28" FontWeight="SemiBold" Foreground="{ThemeResource AppBrandBrush}" Text="{Binding PreviewPrimary}" />
                                        <TextBlock FontSize="13" Foreground="{ThemeResource AppMutedBrush}" Text="{Binding PreviewSecondary}" TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>

                                <StackPanel Spacing="4">
                                    <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource AppTitleBrush}" Text="{Binding Name}" />
                                    <TextBlock FontSize="12" Foreground="{ThemeResource AppMutedBrush}" Text="{Binding Description}" TextWrapping="Wrap" />
                                </StackPanel>

                                <Button Content="添加" Tag="{Binding WidgetType}" Click="OnAddWidgetClick" Style="{StaticResource PrimaryActionButtonStyle}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </ScrollViewer>
    </Grid>
</Page>
